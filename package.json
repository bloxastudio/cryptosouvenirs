{
  "name": "crypto-souvenirs",
  "scripts": {
    "bootstrap": "npm install && npm install --prefix blockchain && npm install --prefix server",
    "bootstrap:ci": "npm ci && npm ci --prefix blockchain && npm ci --prefix server",
    "lint": "npm run lint --prefix blockchain",
    "build": "npm run compile --prefix blockchain && npm run build --prefix server",
    "build:prod": "npm run compile --prefix blockchain && npm run publish --prefix server",
    "test": "npm test --prefix blockchain"
  },
  "devDependencies": {
    "@types/jest": "^27.4.1",
    "@types/node": "^16.11.7",
    "@typescript-eslint/eslint-plugin": "^5.16.0",
    "@typescript-eslint/parser": "^5.16.0",
    "eslint": "^8.12.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.6.1",
    "typescript": "~4.6.3"
  },
  "private": true
}
